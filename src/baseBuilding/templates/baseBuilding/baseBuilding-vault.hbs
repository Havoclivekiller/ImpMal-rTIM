<section class="tab {{tab.cssClass}}" data-group="primary" data-tab="{{tab.id}}">



    <div class="flexrow">

        <div class="attribute-box single solars">
            <div class="label" style="grid-column: 1 / span 4">
                <label class="centered">{{localize "IMPMAL.Solars"}}</label>
            </div>
            <div class="field" style="grid-column: 5 / span 8">
                <input type="number" name="system.solars"
                       value="{{this.system.solars}}">
            </div>
        </div>
    </div>

    <div class="sheet-list">
        <div class="list-header row-content">
            <div class="list-name">{{localize "IMPMAL.Weapon"}}</div>
            <div class="small">{{localize "IMPMAL.Qty."}}</div>
            <div class="small">{{localize "IMPMAL.Enc."}}</div>
            <div class="tiny"></div>
        </div>
        <div class="list-content">
            {{#each items.weapon}}
            {{#unless this.system.isSlotted}}
            <div class="list-row" data-uuid="{{this.uuid}}" data-id="{{this.id}}">
                <div class="row-content">
                    <div class="list-name">
                        <img src="{{this.img}}" class="rollable" data-action="useItem">
                        <a class="label" data-action="toggleSummary">{{this.name}}</a>
                    </div>

                    <a class="small prevent-context" data-action="stepProperty"
                       data-path="system.quantity">{{this.system.quantity}}</a>
                    <div class="small">{{this.system.encumbrance.total}}</div>

                    <div class="list-controls">
                        <a class="list-control context-menu"><i class="fa-regular fa-ellipsis-vertical"></i></a>
                    </div>
                </div>
                {{> actorSlots}}
                {{> sheetButtons document=this}}
                <div class="dropdown-content collapsed">

                </div>
            </div>
            {{/unless}}
            {{/each}}
        </div>
    </div>

    <div class="sheet-list">
        <div class="list-header row-content">
            <div class="list-name">{{localize "IMPMAL.Ammo"}}</div>
            <div class="small">{{localize "IMPMAL.Qty."}}</div>
            <div class="small">{{localize "IMPMAL.Enc."}}</div>
            <div class="tiny"></div>
        </div>
        <div class="list-content">
            {{#each items.ammo}}
            <div class="list-row" data-uuid="{{this.uuid}}" data-id="{{this.id}}">
                <div class="row-content">
                    <div class="list-name">
                        <img src="{{this.img}}" class="rollable" data-action="useItem">
                        <a class="label" data-action="toggleSummary">{{this.name}}</a>
                    </div>

                    <a class="small prevent-context" data-action="stepProperty"
                       data-path="system.quantity">{{this.system.quantity}}</a>
                    <div class="small">{{this.system.encumbrance.total}}</div>

                    <div class="list-controls">
                        <a class="list-control context-menu"><i class="fa-regular fa-ellipsis-vertical"></i></a>
                    </div>
                </div>
                {{> actorSlots}}
                {{> sheetButtons document=this}}
                <div class="dropdown-content collapsed">

                </div>
            </div>
            {{/each}}
        </div>
    </div>

    <div class="sheet-list">
        <div class="list-header row-content">
            <div class="list-name">{{localize "IMPMAL.Modification"}}</div>
            <div class="small">{{localize "IMPMAL.Qty."}}</div>
            <div class="small">{{localize "IMPMAL.Enc."}}</div>
            <div class="tiny"></div>
        </div>
        <div class="list-content">
            {{#each items.modification}}
            <div class="list-row" data-uuid="{{this.uuid}}" data-id="{{this.id}}">
                <div class="row-content">
                    <div class="list-name">
                        <img src="{{this.img}}" class="rollable" data-action="useItem">
                        <a class="label" data-action="toggleSummary">{{this.name}}</a>
                    </div>

                    <a class="small prevent-context" data-action="stepProperty"
                       data-path="system.quantity">{{this.system.quantity}}</a>
                    <div class="small">{{this.system.encumbrance.total}}</div>

                    <div class="list-controls">
                        <a class="list-control context-menu"><i class="fa-regular fa-ellipsis-vertical"></i></a>
                    </div>
                </div>
                {{> actorSlots}}
                {{> sheetButtons document=this}}
                <div class="dropdown-content collapsed">

                </div>
            </div>
            {{/each}}
        </div>
    </div>

    <div class="sheet-list">
        <div class="list-header row-content">
            <div class="list-name">{{localize "IMPMAL.Protection"}}</div>
            <div class="small"></div>
            <div class="small">{{localize "IMPMAL.Qty."}}</div>
            <div class="small">{{localize "IMPMAL.Enc."}}</div>
            <div class="tiny"></div>
        </div>
        <div class="list-content">
            {{#each items.protection}}
            <div class="list-row" data-uuid="{{this.uuid}}" data-id="{{this.id}}">
                <div class="row-content">
                    <div class="list-name">
                        <img src="{{this.img}}" class="rollable" data-action="useItem">
                        <a class="label" data-action="toggleSummary">{{this.name}}</a>
                    </div>

                    {{#if this.system.uses}}
                    <div class="small">
                        {{this.system.uses.value}}
                        {{#if this.system.uses.max}}
                        / {{this.system.uses.max}}
                        {{/if}}
                    </div>
                    {{/if}}

                    <a class="small prevent-context" data-action="stepProperty"
                       data-path="system.quantity">{{this.system.quantity}}</a>
                    <div class="small">{{this.system.encumbrance.total}}</div>

                    <div class="list-controls">
                        <a class="list-control context-menu"><i class="fa-regular fa-ellipsis-vertical"></i></a>
                    </div>
                </div>
                {{> actorSlots}}
                {{> sheetButtons document=this}}
                <div class="dropdown-content collapsed">

                </div>
            </div>
            {{/each}}
        </div>
    </div>

    <div class="sheet-list">
        <div class="list-header row-content">
            <div class="list-name">{{localize "IMPMAL.Forcefield"}}</div>
            <div class="small"></div>
            <div class="small">{{localize "IMPMAL.Qty."}}</div>
            <div class="small">{{localize "IMPMAL.Enc."}}</div>
            <div class="tiny"></div>
        </div>
        <div class="list-content">
            {{#each items.forceField}}
            <div class="list-row" data-uuid="{{this.uuid}}" data-id="{{this.id}}">
                <div class="row-content">
                    <div class="list-name" {{#if this.system.overload.collapsed}} style="color: darkred" {{/if}}>
                        <img src="{{this.img}}" class="rollable" data-action="useItem">
                        <a class="label" data-action="toggleSummary">{{this.name}}</a>
                    </div>

                    {{#if this.system.uses}}
                    <div class="small">
                        {{this.system.uses.value}}
                        {{#if this.system.uses.max}}
                        / {{this.system.uses.max}}
                        {{/if}}
                    </div>
                    {{/if}}

                    <a class="small prevent-context" data-action="stepProperty"
                       data-path="system.quantity">{{this.system.quantity}}</a>
                    <div class="small">{{this.system.encumbrance.total}}</div>

                    <div class="list-controls">
                        <a class="list-control context-menu"><i class="fa-regular fa-ellipsis-vertical"></i></a>
                    </div>
                </div>
                {{> actorSlots}}
                {{> sheetButtons document=this}}
                <div class="dropdown-content collapsed">

                </div>
            </div>
            {{/each}}
        </div>
    </div>


    <div class="sheet-list">
        <div class="list-header row-content">
            <div class="list-name">{{localize "IMPMAL.Equipment"}}</div>
            <div class="small"></div>
            <div class="small">{{localize "IMPMAL.Qty."}}</div>
            <div class="small">{{localize "IMPMAL.Enc."}}</div>
            <div class="tiny"></div>
        </div>
        <div class="list-content">
            {{#each items.equipment}}
            <div class="list-row" data-uuid="{{this.uuid}}" data-id="{{this.id}}">
                <div class="row-content">
                    <div class="list-name">
                        <img src="{{this.img}}" class="rollable" data-action="useItem">
                        <a class="label" data-action="toggleSummary">{{this.name}}</a>
                    </div>

                    {{#if this.system.uses}}
                    <div class="small">
                        {{this.system.uses.value}}
                        {{#if this.system.uses.max}}
                        / {{this.system.uses.max}}
                        {{/if}}
                    </div>
                    {{/if}}

                    <a class="small prevent-context" data-action="stepProperty"
                       data-path="system.quantity">{{this.system.quantity}}</a>
                    <div class="small">{{this.system.encumbrance.total}}</div>

                    <div class="list-controls">
                        <a class="list-control context-menu"><i class="fa-regular fa-ellipsis-vertical"></i></a>
                    </div>
                </div>
                {{> actorSlots}}
                {{> sheetButtons document=this}}
                <div class="dropdown-content collapsed">

                </div>
            </div>
            {{/each}}
        </div>
    </div>

    <div class="sheet-list">
        <div class="list-header row-content">
            <div class="list-name">{{localize "IMPMAL.Augmetic"}}</div>
            <div class="small"></div>
            <div class="small">{{localize "IMPMAL.Qty."}}</div>
            <div class="small">{{localize "IMPMAL.Enc."}}</div>
            <div class="tiny"></div>
        </div>
        <div class="list-content">
            {{#each items.augmetic}}
            <div class="list-row" data-uuid="{{this.uuid}}" data-id="{{this.id}}">
                <div class="row-content">
                    <div class="list-name">
                        <img src="{{this.img}}" class="rollable" data-action="useItem">
                        <a class="label" data-action="toggleSummary">{{this.name}}</a>
                    </div>

                    {{#if this.system.uses}}
                    <div class="small">
                        {{this.system.uses.value}}
                        {{#if this.system.uses.max}}
                        / {{this.system.uses.max}}
                        {{/if}}
                    </div>
                    {{/if}}

                    <a class="small prevent-context" data-action="stepProperty"
                       data-path="system.quantity">{{this.system.quantity}}</a>
                    <div class="small">{{this.system.encumbrance.total}}</div>

                    <div class="list-controls">
                        <a class="list-control context-menu"><i class="fa-regular fa-ellipsis-vertical"></i></a>
                    </div>
                </div>
                {{> actorSlots}}
                {{> sheetButtons document=this}}
                <div class="dropdown-content collapsed">

                </div>
            </div>
            {{/each}}
        </div>
    </div>
</section>
